Contained are solutions for various ProjectEuler problems written in Python.

Problems are sorted in groups of 50, with names euler### to denote the last problem
in each group. The remaining files contain helper functions, which are grouped by
similar usage.